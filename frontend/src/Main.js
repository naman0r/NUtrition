// for universal javascript functions that will be used in all files.

export const handleClick = (message) => {
  console.log(message);
};

// TO-DO
export function getMenuItems(location, meal) {
  console.log("location: ", location, "; meal: ", meal);
}

// TO-DO functions:
